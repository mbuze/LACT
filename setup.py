from setuptools import setup, find_packages

setup(
    name='LACT',
    version='0.0.1',
    description='LAmmps Continuation Techniques. A python-based wrapper around LAMMPS implementing numerical continuation algorithms',
    packages=find_packages()
)
